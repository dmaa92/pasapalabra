@AGENTS.md

# Claude Code

Use the instructions in `AGENTS.md` above as the baseline for this
repository. Prefer shared skills under `.claude/skills/` and
`.agents/skills/` before editing implementation code. Only add
Claude-specific configuration here when a workflow genuinely cannot stay
tool-neutral — default to keeping this file a pointer, not a duplicate.
